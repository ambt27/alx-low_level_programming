#include "main.h"
/**
 * _abs - check if the caracter is lowercase
 * @n: is parameter for the function
 * Return: n if n is positif otherwise return -n
 */
int _abs(int n)
{
	if (n > 0)
		return (n);
	else
		return (-n);
}
